#include<stdio.h>
int main()
{
	int i=1,num,sum=0;
	printf("Display sum of squares of number upto=");
	scanf("%d",&num);
	while(i<=num)
	{
	sum=sum+(i*i);
	i++;
    }
    printf("%d",sum);
    return 0;
}
