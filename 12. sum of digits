#include <stdio.h>
int main()
{
    long rem,sum=0,n;
    printf("Enter any number");
    scanf("%d",&n);
    while(n!=0)
    {
    	rem=n%10;
    	sum=rem+sum;
    	n=n/10;
	}
	printf("The sum of digits ofgiven number is %ld.",sum);
    return 0;
}
