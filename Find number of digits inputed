#include<stdio.h>
int main()
{
	int num,count;
	printf("Input number whose no. of digits is to be found?");
	scanf("%d",&num);
	while(num!=0)
		{
		num=num/10;
		
		count++;
	 }
	printf("The number of inputed digits are %d",count);
	return 0;
}
	
